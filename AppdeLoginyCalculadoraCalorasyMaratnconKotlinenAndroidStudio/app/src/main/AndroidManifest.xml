<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppDeLoginYCalculadoraCaloríasYMaratónConKotlinEnAndroidStudio"
        tools:targetApi="31">

        <!-- MainActivity (actividad principal) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppDeLoginYCalculadoraCaloríasYMaratónConKotlinEnAndroidStudio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CaloriasActivity -->
        <activity
            android:name=".CaloriasActivity"
            android:exported="false"
            android:label="Calculadora de Calorías"
            android:theme="@style/Theme.AppDeLoginYCalculadoraCaloríasYMaratónConKotlinEnAndroidStudio" />

        <!-- BostonActivity -->
        <activity
            android:name=".BostonActivity"
            android:exported="false"
            android:label="Clasificación Maratón"
            android:theme="@style/Theme.AppDeLoginYCalculadoraCaloríasYMaratónConKotlinEnAndroidStudio" />
    </application>

</manifest>
